# State files (runtime data, gitignored)
state/*.json
state/*.ndjson
!state/.gitkeep

# Examples are committed (show structure, not real data)
!examples/*.example.json
!examples/*.example.ndjson
!examples/fixtures/**

# macOS
.DS_Store
